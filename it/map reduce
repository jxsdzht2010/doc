    Google提出的Map Reduce，本人粗浅的理解。
    有一个list [1, 2, 3, 4, 5]
    两种Map操作
    +1 -> [2, 3, 4, 5, 6]
    *2 -> [2, 4, 6, 8, 10]
    两种Reduce操作
    sort -> [5, 4, 3, 2, 1]
    sum  -> [15]
    
    Map操作对列表中每个元素是独立的，而Reduce操作是元素之间存在联系。
    如果将Reduce操作转换成Map操作，那么就可以将任务并行化了。